apiVersion: v1
kind: Service
metadata:
  name: batch
  namespace: batch
  labels:
    app.kubernetes.io/name: batch
spec:
  selector:
    app.kubernetes.io/name: batch
  ports:
    - name: http
      port: 8080
      targetPort: http
