apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: apps
  namespace: cicd
spec:
  description: Application layer (MSA services)
  sourceRepos:
    - https://github.com/GGingGGang/k8s-gitops.git
  destinations:
    - server: https://kubernetes.default.svc
      namespace: cicd
    - server: https://kubernetes.default.svc
      namespace: core
    - server: https://kubernetes.default.svc
      namespace: batch
    - server: https://kubernetes.default.svc
      namespace: login
  clusterResourceWhitelist: []
  namespaceResourceWhitelist:
    - group: "*"
      kind: "*"
